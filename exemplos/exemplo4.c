int sub( int a, float b){  
    int c;
    c = a - b;
    float b;
    elem vs;

    if(a > b){
        vs = a * 3.4;
    }

    return a ** $b;
}

float soma(int x, float a){

    float res;

    res = sub(x, a, df);

}