int func(int f, int g) {
    int u;
    u = f + g;
    writeln(u);
    return u;
}

int main () {
    int a, c;
    int b;
    float x;
    a = -10;
    b = 2;
    // if(a && b){
    //     writeln(a);
    // }else if( a || b) {
    //     writeln(a);
    // }
    // if(a != b){
    //     writeln(a);
    // }
    // }else if( a < b) {
    //     writeln(b);
    // }else if( a <= b) {
    //     writeln(b);
    // }else if( a >= b) {
    //     writeln(a);
    // }
    // c = func(a, b);
    // (a / b) * a;
    // writeln(func(a, b));
    read("c");
}