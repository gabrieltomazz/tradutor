int funca( int x, float y){

    for (i = 3; !(add(1 in i)) && 3 < 4; i = i + 1) {
        set b;
        b = 1;
    }

    elems bfds;
    write("as");
}

int main(int a, int s){
    float h, b;
    a = -5.4;

    is_set(a);
    add(a in b);

    if(a > b) {
        float v;
        v = b + 2.1;
    }else {
        a = -b + 3;
    }

    func(a, b);

    return a;
}

int abc( int j, float u){

    forall(x in s) {
        int acc;
        float accf;
        acc = acc + x;
        accf = accf + x;
    }

    write("aqui");
}