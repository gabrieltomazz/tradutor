int funca( int i, float y, elem h){

    for (i = 3; !(add(1 in i)) && 3 < 4; i = i + 1) {
        set b;
        b = 1;
    }

    elem bfds;
    write("as");
}

int abc( int x, float s){

    forall(x in s) {
        int acc;
        float accf;
        acc = acc + x;
        accf = accf + x;
    }
    writeln("aaa");
}

int main(){
    float h, b, a;
    a = -5.4;

    is_set(a);
    add(a in b);

    if(a > b) {
        float v;
        v = b + 2.1;
    }else {
        a = -b + 3;
    }

    funca(a, b, h);

    return a;
}