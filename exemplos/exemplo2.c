int abc( int x, float s){
    int c;
    forall(x in s) {
        int acc;
        float accf;
        acc = acc + x;
        accf = accf + x;
    }
    writeln("aaa");
}
int t, u, gf, rew;
int main( int a, int t, int v){  
    int x, c, d, i;
    c = a - d;
    abc(a, c);
    exists(t in a);
    // remove(e in r);
    return x;
}