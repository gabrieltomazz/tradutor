int sub( int a, float b){  
    int c;
    c = a - b;

    set possibleSums;
    possibleSums = EMPTY;
    int x;

    forall(x in s) {
        acc = acc + x;
        accf = accf + x;
    }

    exist(x in s);

    return x;
}