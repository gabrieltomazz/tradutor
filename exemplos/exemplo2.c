int abc( int x, float s){
    int c;
    forall(x in s) {
        int acc;
        float accf;
        acc = acc + x;
        accf = accf + x;
    }
    writeln("aaa");
}
// int t, u, gf, rew;
// elem gh, uf;
// set vb, vj;
int main( int a, int t, int v){  
    // int x, c, i;
    // float d;
    // c = a + d * 3 ;
    float b;
    int d; 
    int c;
    b = 2;
    c = d + c * 2.5;
    // v = abc(a, c);
    // abc(a, c);
    // gh = uf + gh;
    // vb = vb + vj;
    // exists(t in a);
    // remove(e in r);

    return c;
}